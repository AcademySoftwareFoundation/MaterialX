void mx_noise3d_color4(vector4 amplitude, float pivot, vector position, output color4 result)
{
    color4 value = noise("perlin", position);
    result = value * color4(amplitud.x, amplitud.y, amplitud.z, amplitud.w) + pivot;
}
