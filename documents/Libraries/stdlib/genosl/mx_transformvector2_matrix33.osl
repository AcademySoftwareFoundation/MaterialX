void mx_transformvector2_matrix33(vector2 val, matrix m, output vector2 result)
{
    vector _val = vector(val.x, val.y, 1.0);
    vector res = transform(m, _val);
    result.x = res[0];
    result.y = res[1];
}
